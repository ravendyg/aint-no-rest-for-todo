'use strict';

import Vue from 'vue';

export default Vue.component(
  'todo-item',
  {
    render(h, context) {
      return (
        <div>
          Not implemented
        </div>
      );
    }
  }
);
