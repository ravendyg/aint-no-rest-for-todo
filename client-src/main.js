'use strict';

import Vue from 'vue';

export default Vue.component(
  'main-route',
  {
    render(h, context) {
      return (
        <div>
          Hello, world!
        </div>
      );
    }
  }
);
